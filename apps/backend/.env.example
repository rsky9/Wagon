# Backend
DATABASE_URL="postgresql://wagon:wagon_dev@localhost:5440/wagon?schema=public"
PORT=4020
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"

# Redis
REDIS_URL="redis://localhost:6380"

# MinIO (dev)
MINIO_ENDPOINT="localhost:9010"
MINIO_ROOT_USER="wagon"
MINIO_ROOT_PASSWORD="wagon_dev_secret"
MINIO_BUCKET="wagon"

# Mobile
FCM_PROJECT_ID=""

# Mock OTP: in production, wire a real SMS/WhatsApp gateway behind OtpProvider
OTP_PROVIDER="mock"

# JWT (rotate these in production; keep dev values here)
JWT_ACCESS_SECRET="wagon_dev_access_secret_change_me"
JWT_REFRESH_SECRET="wagon_dev_refresh_secret_change_me"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="30d"
